package syswin.fences.services.gsm;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class GSMListener extends Thread {

    private final static Logger log = LogManager.getLogger (GSMListener.class.getName ());

    public void run () {

    }
}
